# File structure

for 

for source code
src/
    ->input.css(for css) for front end
    ->php
        ->classes (detailed)
        ->include
    ->form-handling (abstracted)
        ->login
        ->register